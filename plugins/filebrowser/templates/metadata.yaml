apiVersion: v1
kind: ConfigMap
metadata:
  name: "{{ .Release.Name }}-terra-metadata"
  labels:
    kuiper.juno-innovations.com/chart: "{{ .Release.Name }}-scripts-configmap"
data:
  chart: "{{ .Release.Name }}-scripts-configmap"
  description: "Web-based file browser with drag-and-drop upload. Mount workspace volumes to browse and upload files."
  fields: |
    - name: registry
      description: "Container registry"
      required: true
      default: "docker.io"
    - name: repo
      description: "Container image repository"
      required: true
      default: "filebrowser/filebrowser"
    - name: tag
      description: "Container image tag"
      required: true
      default: "latest"
    - name: ingressNamespace
      description: "Ingress controller namespace. Default: ingress-nginx"
      required: false
      default: "ingress-nginx"
    - name: cpu
      description: "CPU request"
      required: true
      default: "512m"
    - name: memory
      description: "Memory request"
      required: true
      default: "256Mi"
    - name: root
      description: "Root filesystem path visible in FileBrowser. Set to '/' to browse the entire container filesystem, or '/data' to restrict to the data volume only."
      required: false
      type: string
      default: "/"
    - name: icon
      default: "https://github.com/juno-fx/Terra-Official-Plugins/blob/main/plugins/filebrowser/scripts/assets/icon.png?raw=true"

