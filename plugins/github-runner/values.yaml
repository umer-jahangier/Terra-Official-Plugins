# Default values for the plugin install (Terra install-time only).
# No install-time fields: all configuration happens at workload launch time
# (fields in templates/metadata.yaml, passed to scripts/chart/ by Kuiper).